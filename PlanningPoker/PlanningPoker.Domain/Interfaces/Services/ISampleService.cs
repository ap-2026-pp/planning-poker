using PlanningPoker.Domain.DTOs;

namespace PlanningPoker.Domain.Interfaces.Services;

public interface ISampleService
{
    // Task<SampleDto> CreateAsync(CreateSampleDto dto, CancellationToken ct = default);
    // Task<SampleDto?> GetAsync(int id, CancellationToken ct = default);
    // Task<IReadOnlyList<SampleDto>> ListAsync(CancellationToken ct = default);
}